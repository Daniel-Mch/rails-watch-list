<div class="container">


<h1>Create list</h1>

<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>

  <div class="btn btn-primary">
   <%= f.button :submit  %>
  </div>

<% end %>

  <%= link_to "Back to lists", lists_path  %>

</div>
