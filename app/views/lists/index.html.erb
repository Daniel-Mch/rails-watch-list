<div class="container">
  <h1>Lists</h1>

  <% @lists.each do |list|  %>
    <div class="card">
     <%= link_to list.name, list_path(list) %>
    </div>
  <% end %>

  <%= link_to "Create a list", new_list_path %>



</div>
